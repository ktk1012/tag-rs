[package]
name = "tag-rs"
version = "0.1.0"
edition = "2024"
description = "Tag your search results with numbered aliases"
license = "MIT"

[dependencies]
is-terminal = "0.4"
owo-colors = "4"
strip-ansi-escapes = "0.2"
regex = "1"

[profile.release]
strip = true
lto = true
